<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AND102_ASM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Add the following item to make the status bar transparent -->
<!--        <item name="android:background">@color/white</item>-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Change default font family -->

        <!--<item name="android:fontFamily">@font/dosis</item>
        <item name="android:fontVariationSettings">"wght" 700</item>
        <item name="android:fontStyle">normal</item>
        <item name="android:fontWeight">700</item>
        <item name="android:textFontWeight">700</item>
        <item name="android:font">@font/dosis</item>-->

    </style>

    <style name="Theme.AND102_ASM" parent="Base.Theme.AND102_ASM" />
</resources>